// function will be used in src/index.js

// print.js will be added as a new entry point. An entry point is the file that will be fed into the bundle. The output bundle can be dynamically named based on entry point names

export default function printMe(){
    console.log('I get called from print.js');
}